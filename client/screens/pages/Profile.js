import { View, Text, SafeAreaView } from "react-native";

const Profile = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <View>
        <Text>Profile</Text>
      </View>
    </SafeAreaView>
  );
};

export default Profile;
